@page "/chargen"
@using ArkLens.Models.Drafts;
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.WebAssembly.Authentication
@using Un1ver5e.Site.Client.Shared.СharacterElements
@inject AuthenticationStateProvider Auth

<PageTitle>CharGen</PageTitle>

<CharacterListComponent
	Draft="Draft"/><br />

<NameComponent
	Draft="Draft"/>

<AlignmentComponent
	Draft="Draft"/>

<RaceComponent
	Draft="Draft"/>

<SexComponent
	Draft="Draft"/>

<SvgComponent
	Draft="Draft"/>

<CharacterStorageComponent
	Draft="Draft"/>

@code {
	private CharacterDraft Draft { get; set; } = new();
	private AuthenticationState AuthState { get; set; } = null!;

	protected override async Task OnInitializedAsync()
	{
		AuthState = await Auth.GetAuthenticationStateAsync();
	}
}